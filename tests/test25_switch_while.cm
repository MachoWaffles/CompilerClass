function Master() returns nothing {
    int x = 1;

    while (x < 4) {

        switch (x) {

            case 1:
                print(100);
                break;

            case 2:
                print(200);
                break;

            case 3:
                print(300);
                break;
        }

        x = x + 1;
    }
}
